scikit-learn>=1.3
scikit-image>=0.21
opencv-python>=4.8
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
joblib>=1.3

# Web UI for single-image testing (app.py)
streamlit>=1.30
pillow>=10.0

# Tomato model only. Deliberately left out of the default install: TensorFlow is
# a very large dependency and the bell pepper half of the app does not need it.
# app.py imports it lazily and shows an install prompt if you pick Tomato
# without it.
#   pip install tensorflow
