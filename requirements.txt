scikit-learn>=1.3
scikit-image>=0.21
opencv-python>=4.8
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
joblib>=1.3

# Web UI for single-image testing (app.py)
streamlit>=1.30
pillow>=10.0

# Tomato model only. Left out of the default install because it is large and the
# bell pepper half does not need it; app.py imports it lazily and shows an
# install prompt if you pick Tomato without it.
#
# Use Keras 3 + JAX, NOT TensorFlow. tomato_model.keras was saved by Keras 3.13
# in the backend-agnostic .keras format, and TensorFlow publishes no wheels for
# Python 3.14 (`pip install tensorflow` fails with "from versions: none"). Keras
# on JAX loads the identical file.
#   pip install keras jax jaxlib
